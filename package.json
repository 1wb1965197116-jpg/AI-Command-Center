{
  "name": "ai-command-center",
  "version": "1.0.0",
  "scripts": {
    "validate": "node scripts/validate.js",
    "install:all": "npm run validate && npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "node server/server.js"
  }
}
